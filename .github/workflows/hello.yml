name: Hello World

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  run-hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Hello World
        run: python hello.py
